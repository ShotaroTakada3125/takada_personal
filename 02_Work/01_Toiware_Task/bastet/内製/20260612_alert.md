# EC2接続, 作業ディレクトリ移動
cd ~/report
mkdir 20260612_datapatch
cd 20260612_datapatch

DB接続

\echo :AUTOCOMMIT
\pset null '[NULL]'
\o |tee report.bef.log

# 対象のデータを表示
select * from individual_account where account_id = '18391893';

exit


DB接続
\echo :AUTOCOMMIT
\pset null '[NULL]'
\o |tee report.aft.log

(\set AUTOCOMMIT off)

begin;

# 対象のgiven_nameをupdate
UPDATE individual_account SET given_name = REPLACE(given_name, '\', '') WHERE account_id = '18391893';

# 対象のデータを表示
select * from individual_account where account_id = '18391893';

commit;
exit